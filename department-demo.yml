server:
  port: 8080

spring:
  application:
    name: department-demo

  datasource:
    url: jdbc:mysql://localhost:3306/department_demo_1?createDatabaseIfNotExist=NO
    username: root
    password: th15@rud@51th

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


message: hello world
